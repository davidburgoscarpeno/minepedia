---
import GuideDetail from '../../components/pages/GuideDetail.astro';
import guides from '../../data/guides.json';
export function getStaticPaths() {
  return guides.map(g => ({ params: { slug: g.slug }, props: { g } }));
}
const { g } = Astro.props;
---
<GuideDetail g={g} lang="en" />
