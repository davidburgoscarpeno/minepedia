---
import RecipeDetail from '../../components/pages/RecipeDetail.astro';
import recipes from '../../data/recipes.json';
export function getStaticPaths() {
  return recipes.map(r => ({ params: { slug: r.slug }, props: { r } }));
}
const { r } = Astro.props;
---
<RecipeDetail r={r} lang="en" />
